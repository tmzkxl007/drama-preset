# -*- coding: utf-8 -*-
"""포레이로 한 편의 설계도. 이 파일을 편 폴더에 `episode.py` 로 복사해서 채운다.

★읽고 시작해라: `presets/포레이로/README.md` 3장(나레이션 원고)과 5장(컷).
  규격은 `spec.py`, 근거는 `docs/PLAYBOOK.md` §17.
"""

SRC = "src.mp4"          # 편 폴더에 둔 소재 원본
OUT = "ep01.mp4"         # 완성본 이름

# ── 제목 두 행 ──────────────────────────────────────────
# 1행 = 수식·조건절(살구색) / 2행 = 주어·인물(흰색). ★전체가 명사구로 끝나야 한다.
#   보기: "괴물 실험을 위해 희생된" / "마루타 부대의 조선인들"
HEAD1 = ""
HEAD2 = ""

WORK = ""                # 설명문에 넣을 작품명 (`* 작품: OO`)

# 원본에 박힌 자막 띠의 위쪽 y. 있으면 그 위만 쓴다. 없으면 None.
HARDSUB_TOP = None

# ── 뼈대 ────────────────────────────────────────────────
# ("N", "나레 문구")            나레 마디. 길이는 TTS 가 정한다. 그림은 CUTS 에서 채운다.
# ("D", 시작초, 끝초, "자막")    원본 대사. 그 구간의 그림과 소리를 그대로 쓴다.
#
# ★N 이 10~15개, D 는 2~6개다. D 가 많아지면 이 프리셋이 아니다(§17-6).
# ★모든 N 문구를 연결어미로 잇고, **마지막 N 도 연결어미로 끊는다.**
#   -고 / -는데 / -이었고 / -지만 / -하며 / -자 / -죠
# ★3인칭 해설. 대명사("그는") 대신 명사로 인물을 지목한다.
BLOCKS = [
    # ("N", "조선인을 상대로 생체 실험을 하던 마루타 부대에 퇴각 명령이 떨어지고"),
    # ("N", "실험 흔적을 없애기 위해 모든 자료를 소각하는데"),
    # ("D", 470.0, 473.2, "48시간 안에 모든|퇴각 준비를 끝낸다"),
]

# ── 그림 ────────────────────────────────────────────────
# N 구간을 채울 컷. (시작초, 끝초). 순서가 곧 편집 순서 — **시간 순서를 무시해도 된다.**
#
# ★0번이 훅이다. 그 편에서 **가장 센 그림**을 넣어라. 이 한 칸이 조회수를 가른다(§17-9).
# ★첫 5초는 0.3~1.0초짜리로 4~6컷 몰아치고, 그 뒤는 1.0~3.0초로 늦춘다.
CUTS = [
    # (470.0, 470.6),
    # (12.0, 12.5),
]

# ── 효과자막 (분홍) ─────────────────────────────────────
# (완성본 기준 초, "문구", 자리)  자리: cap / upper / left / right
# 편당 2~5개. 감탄·비명·의성어·결정적 대사에만. 0초에 하나 얹으면 좋다.
EFFECTS = [
    # (0.0, "오! 여신의 탄생이다..!!", "cap"),
]
